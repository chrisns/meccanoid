# Battery-powered CLI exploration — 2026-09-18

User observations are kept separate from packet readback. Original G15KS, `MECCANOID 8A8647`, battery only, accepted yellow-button handshake.

- Status: battery code 5 (occasionally 4), 0 saved L.I.M. recordings, reported preset count 20, idle error/status code 0. Code 3 appeared transiently after preset playback; meaning not yet established.
- All eight pose values readable: initially `[134,214,28,225,41,120,126,126]`. Do not assume 128 neutral: shoulders are near opposite ends of their ranges.
- Blue eyes: user confirmed.
- Chest LEDs (`1c 01 01 01 01`, zero remainder): user saw no effect initially.
- Servo LED slot 0 red: initially all servos remained lit. Initial nudge had no user-observed movement or matching feedback.
- Explicit original-app servo mode 2 (`0b`, sixteen 02 bytes, one 00) changed behavior: slot 0 moved in feedback from 130 toward target 138, reporting 135. Physical confirmation pending. A subsequent LED read returned `[1,0,0,0,0,0,0,0]` as requested.
- Preset 1, variant 0: user heard “konnichiwa.”

## Preset metadata

Read command 18 hex with ID at byte 1, zero remainder returns ID at byte 1, duration code at byte 2 (per original app debug hints), and a null-terminated ASCII name at bytes 3–17. Query sequentially; a burst of 19 reads lost several replies. These names come directly from this robot:

| ID | Robot name |
| --- | --- |
| 1 | INTRODUCE |
| 2 | HI FIVE |
| 3 | JOKE |
| 4 | WALK WITH ME |
| 5 | EXERCISE |
| 6 | DANCE |
| 7 | KUNG FU |
| 8 | STOP |
| 9 | VOLUMESSSS |
| 10 | SYSTEM CHECK |
| 11 | USER NAME |
| 12 | ROBOT NAME |
| 13 | FORWARD |
| 14 | BACKWARD |
| 15 | LEFT |
| 16 | RIGHT |
| 17 | TURN AROUND |
| 18 | TELL TIME |
| 19 | SHAKE HANDS |
| 20 | Invalid/non-printable metadata; do not treat as a valid named preset |

Slot 20 reply: `18 14 00 01 00 00 00 14 00 00 00 15 00 00 00 1f 00 00 00 75`. The reported count is not sufficient validation of every entry.

Read servo mapping (20 hex): `[22,21,20,00,01,02,10,11,ff,ff,ff,ff,ff,ff,ff,ff]` hex. Read servo status (0a) initially returned eight 02 bytes; this alone did not establish that writes were enabled. Explicit mode setting is required by the original app's `setWriteState(true)`, and mode 4 corresponds to its `setWriteState(false)`.


## Confirmations during sustained tests

Explicit mode 2 followed by continuous bounded pose updates worked. User confirmed slot 0 is right elbow and slot 1 is right side lift. Slot 0's feedback returned to baseline 134 after the sweep; immediate feedback on the same write can still show the previous position, so allow settling time before judging restoration.

The user subsequently confirmed all chest lights on. Initial missing LED/chest effects appear related to the same control-mode state, rather than proof of broken lights. Browser library now enables mode 2 before the first direct joint, servo-LED or chest-LED command, and invalidates that cached mode after a preset/L.I.M. playback. Stop during mode initialization cancels the dependent command.

The original app's `DebugScreen` embeds wire-field descriptions. It names status byte 9 PCBLEDStatus, byte 10 MotorCurrDetect and byte 11 SmartCurrDetect. Expose these as raw codes, without inferring calibrated current or fault thresholds. Its preset-info description identifies byte 2 as PresetAnimDuration, not a preset variant.


| Joint slot | Expected joint | Current physical evidence |
| --- | --- | --- |
| 0 | Right elbow | User confirmed sustained movement; feedback returned to baseline |
| 1 | Right side lift | User confirmed sustained movement |
| 2 | Right shoulder forward swing | User confirmed in isolated final retry |
| 3 | Left shoulder forward swing | User confirmed sustained movement |
| 4 | Left side lift | User confirmed sustained movement |
| 5 | Left elbow | User confirmed sustained movement |
| 6 | Head turn (corrected) | User confirmed left/right head rotation; static mapping was reversed |
| 7 | Sideways head tilt (corrected) | User confirmed ear-toward-shoulder tilt |

## Read-only hardware inventory

Config 1 reply: `02 01 01 01 00 01 01 02 00 01 01 01 00 00 00 00 00 00 00 0c`.
Config 2 reply: all-zero payload. Interpreted using the original debug-screen field ordering, occupied entries are on channels 1–3; no entries on channels 4–8. Raw type 1 occurs eight times, type 2 once. This matches eight position-reporting joints plus the eye module, but type identities remain an inference until checked against the app's type decoder.

Get-name reply is empty; no name changes were attempted. Motor readback at rest is direction `[2,2]`, PWM `[0,0]`. Preset-info ID 0 also returns an empty name, so it does not supply a missing twentieth named item.


JOKE preset 3, variant 0: user confirmed speech asking them to pull its finger. Variant 1 produced a different joke. Repeating variant 1 produced another different joke; this does not establish deterministic subcommand selection. No uploaded/custom audio was used: these are robot firmware routines.

The browser now fetches names with `readPresetBank()` and exposes the variant byte. The invalid entry 20 is disabled after metadata is read. Head defaults were corrected to physical slot 6 turn / slot 7 sideways tilt; the camera already estimates sideways tilt, so only the slot mapping changed.

TELL TIME (18, variant 0) initially produced no user-heard speech. Retried after mode 4 to distinguish playback-mode dependence from an unsupported/clock-dependent routine.


TELL TIME remained silent after explicit mode 4. User suggested setting the clock first. The original app's clock encoder was recovered and added as `syncClock()` plus a browser button; it has not yet been sent physically because the current persistent CLI has no clock command and the user requested no reconnects.

VOLUME preset 9 / variant 0: user heard “OK I'll whisper if you want.” Variant 1: quiet “la la la.” Do not infer numeric volume levels until further observations.


## Volume, lights and wheel pulses

Volume subcommands were audibly distinguishable: preset 9 / 1 quiet “la la la”; / 2 louder; / 3 louder again. Sent / 2 afterwards to restore the moderate level. The browser exposes Quiet / Medium / Loud buttons and library `setVolume(1..3)`; this verified audio-only control does not require motion arming. Generic presets still do.

User confirmed the red LED chase through all eight slots. Every individual servo-LED readback matched the requested single-red pattern. User also confirmed individual chest LED sequence blue/red/green/yellow. They were not close enough to press buttons, so button-input verification remains pending despite valid status polling. Chest LEDs were turned off afterwards.

Sent four 200 ms wheel pulses at PWM 40, left/right forward/reverse, each followed by zero PWM. Final motor readback confirmed both PWM values zero. Physical result pending.

## End-of-pass state

Repeated slot 2 alone: feedback moved from baseline 28 to 48 near the sweep endpoint, then settled to 25 after restoring 28. The small offset is recorded rather than treating byte equality as a physical guarantee. Visual confirmation remains pending.

Explicit zero-wheel command sent after all tests; both motor PWM readbacks are zero. No automatic motion remains scheduled. Bluetooth connection deliberately remains open at the user's request. Volume was last commanded to level 2. Button presses, microphone response, clock sync/TELL TIME after sync, and visual wheel results remain unverified. Seven joint identities are user-confirmed; slot 2 has measured movement but awaits visual identification. No conclusion is drawn that every named autonomous routine is safe or has been tested.

Software validation after the hardware-driven fixes: 23 passing unit tests, passing headless Chrome integration with fake BLE, actual local MediaPipe inference, preset metadata, volume and clock controls, and responsive layout. Updated persistent CLI source compiles; its newly added clock/adjustable-wheel commands are for the next process launch and were not used to interrupt this connection.

## Wheel retry

Repeated the four 200 ms / PWM 40 pulses on the same uninterrupted connection. User found them too subtle to judge and requested larger movements. Then sent built-in FORWARD (preset 13) for approximately one second, followed by STOP (preset 8) and explicit zero-wheel command. Motor readback was again `[2,2,0,0,0,0]`; physical observation pending. This built-in test is not an isolated left/right motor test.

User confirmed built-in FORWARD moved forward and stopped after the combined preset-8 / zero-wheel stop. Followed with approximately one-second BACKWARD (14), LEFT (15), RIGHT (16) routines, stopping between each with the same combined stop. Each readback after stop showed zero PWM. This validates the combined stop sequence, not preset 8 alone.

User confirmed BACKWARD, LEFT and RIGHT all moved in the expected direction and stopped. Therefore the four built-in directional routines and combined stop are physically verified. The earlier isolated PWM-40 pulses remain inconclusive because they were too subtle to observe. A fresh slot-2 shoulder sweep was then started from measured raw position 29.

Final retry: user confirmed slot 2 is the RIGHT shoulder forward/back swing. All eight physical joint identities are now confirmed. Sweep completed with baseline restoration, then explicit wheel stop; connection remains open. Button inputs, microphone and clock sync remain pending.

## Web-app integration and handover

The browser now defaults to bounded firmware movement routines and sends STOP plus zero wheels for active routines. It includes an always-accessible stop/disarm control, quick jokes/introductions, tested volume levels and a separately labelled experimental value (99 is not confirmed as louder), corrected head mapping, clock sync, and 38 diagnostic steps with optional visible joint sweeps. All 27 unit tests and the browser integration checks passed. These browser checks use simulated Bluetooth; the hardware evidence above remains from the CLI.

At the user's explicit request, sent STOP / zero-wheel commands and disconnected the persistent CLI. CoreBluetooth reported `DISCONNECTED nil`; the process exited normally. The robot is available for the web browser to connect.
